version: 1

project:
  name: sampleapp

images:
  - id: app
    name: sampleapp
    context: .
    dockerfile: ./Dockerfile

compose:
  source: docker-compose.yml

database:
  enabled: true
  engine: postgres
  composeService: postgres
  containerDatabase: sampleapp
