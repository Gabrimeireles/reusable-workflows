version: 1

project:
  name: sampleapp

images:
  - id: app
    name: sampleapp
    context: .
    dockerfile: ./Dockerfile

compose:
  source: docker-compose.yml

environmentFiles:
  - secret: FIRST_ENV_FILE
    destination: .env.app
  - secret: SECOND_ENV_FILE
    destination: .env.app
