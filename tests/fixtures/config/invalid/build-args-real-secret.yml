version: 1

project:
  name: sampleapp

images:
  - id: app
    name: sampleapp
    context: .
    dockerfile: ./Dockerfile
    buildArgs:
      STRIPE_SECRET_KEY: "sk_live_should_never_be_a_build_arg"

compose:
  source: docker-compose.yml
