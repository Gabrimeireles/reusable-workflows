# One "owner/repo" per line. Blank lines and lines starting with # are ignored.
#
# Copy this file to scripts/sync-secrets.repos (gitignored — repo names aren't secret, but this
# keeps the maintained list a local, editable artifact rather than something every project
# addition requires a commit here for) and list every repo that shares the account-level
# secrets (GHCR_PAT, TS_OAUTH_CLIENT_ID, TS_OAUTH_SECRET, DEPLOY_HOST, DEPLOY_USER,
# DEPLOY_SSH_KEY) synced by sync-secrets.sh.
Gabrimeireles/Foundrix
Gabrimeireles/Pricely
